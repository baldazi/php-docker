version: '3.8'

services:
  php:
    image: php:8.2-apache
    container_name: php82Learn
    ports:
      - 8000:80
    volumes:
      - ./public:/var/www/html
    build: 
      context: .
      dockerfile: dockerfile
      

    